# The demo Prometheus, marked default so the dashboards' $datasource variable
# resolves to it automatically (no hardcoded uid in the dashboards).
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
