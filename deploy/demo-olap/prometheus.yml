# Prometheus config for the demo-olap stack. Scrapes latkit's /metrics on the
# compose network every 5s so the dashboards come alive quickly.
global:
  scrape_interval: 5s
  scrape_timeout: 4s

scrape_configs:
  - job_name: latkit
    static_configs:
      - targets: ["latkit:9752"]
